<%= simple_form_for [@topic,@post, Comment.new] do |f| %>
  <%= f.input :body, label: 'Leave a comment', placeholder: "Enter your comment here."%>
  <%= f.submit 'Submit' , class: 'btn btn-success' %>
<% end %>
