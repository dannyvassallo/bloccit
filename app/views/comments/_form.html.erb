<%= simple_form_for [@topic,@post,@post.comments.new] do |f| %>
  <%= f.input :body, label: 'Body', placeholder: "Post a Comment", :input_html => { 'rows' => 8 }  %>
  <%= f.submit 'Submit' , class: 'btn btn-success' %>
<% end %>
